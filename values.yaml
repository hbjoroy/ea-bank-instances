# Isdalen Bank — instance overrides
# All other values inherit from the bjoroy-bank chart defaults

bjoroy-bank:
  global:
    instanceId: "isdalen"
    domain: "bjoroy.me"
    imageTag: "1.4.13"
    imageRegistry: "oci.bjoroy.me/bank"
    dbHost: "postgres-postgresql.database.svc.cluster.local"

  paymentEngine:
    centralBankUrl: "http://bragd-api.bragd-rtgs.svc.cluster.local:8095"
    bankId: "ISDA"

  ingress:
    enabled: true
    className: traefik
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - host: isdalen.bjoroy.me
        paths:
          - path: /
            pathType: Prefix
      - host: isdalen-admin.bjoroy.me
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: isdalen-tls
        hosts:
          - isdalen.bjoroy.me
          - isdalen-admin.bjoroy.me
